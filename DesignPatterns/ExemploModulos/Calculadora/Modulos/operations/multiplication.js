module.exports = {
	/**
	 * Funcao de multiplicacao
	 * @param number x
	 * @param number y
	 * @return number result
	 */
	multiplication: async (x, y) => {
		let result = x * y;
		return result;
	},
};
